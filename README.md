# CompPacks Releases

Public release manifest for the CompPacks AE plugin. Panels read `latest.json` from this repo via raw.githubusercontent.com / jsDelivr / GitHub API.

**Do not commit anything other than `latest.json` and the current versioned `-update.zip`.**
